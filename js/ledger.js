// Ledger/LocalDB helper for demonstration purposes if backend is unavailable
window.JanSamadhanData = {
    // This file is kept for backward compatibility with the original prototype structure
    // but now functions are redirected to fetch from real API where possible.
};
